name: smart_classroom_new
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  http: '0.13.6'
  file_picker: ^8.0.0
  flutter_pdfview: ^1.3.2
  path_provider: ^2.1.0
  firebase_core: ^3.0.0
  firebase_messaging: ^15.0.0
dependency_overrides:
  web: '0.5.1'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true